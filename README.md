## Database
Insert MySQL password to server.py
Run databse scripts

## TO RUN THE SERVER:
Install Dependencies (pip3 flask, flask-cors, mysql, datetime)
Run the Code in backend folder (python3 server.py)


## TO RUN THE CLIENT:

Run `npm i` to install the dependencies.
Run `npm run dev` to start the development server.

